# Zhixie's Page

个人博客，托管于 **GitHub Pages**。纯原生 HTML / CSS / JS，零框架、零构建。

- 主站：<https://uzjhf-836.github.io/>
- 博客：<https://uzjhf-836.github.io/blogs/index.html>

## 技术特点

- **数据驱动**：文章列表由 `blogs/文章.js` 一条记录渲染，加文章 = 加一条数据
- **单模板文章页**：所有文章共用 `blogs/article.html`，正文独立存为 `blogs/articles/N.md`，页面 `fetch()` 后由 `marked` 渲染（支持 Markdown 混写原生 HTML）
- **无后端**：阅读进度、主题切换、收藏、排序全部前端实现，URL 参数控状态、Cookie 存偏好
- **浅色 / 深色双主题**

## 目录结构

```
├── index.html            # 首页
├── 404.html
├── css/                  # 全站样式
├── blogs/
│   ├── index.html        # 博客列表页
│   ├── article.html      # 文章页通用模板（?id=N 取文章）
│   ├── articles/         # 文章正文（N.md）
│   ├── 文章.js           # 文章元数据 + 样式徽标定义
│   ├── badge-colors.css  # 徽标配色
│   ├── create_article.py # 文章生成脚本（本地专用，已 gitignore）
│   └── test_server.py    # 本地预览服务器（本地专用，已 gitignore）
├── project/              # 项目页
├── chess/ egg/ font/ sources/ docs/   # 其他子站/素材
```

## 博客运作原理

1. **列表页** `blogs/index.html` 读 `文章.js`，按记录渲染卡片；支持 `?sort=` 排序、`?pin=0` 关闭置顶优先
2. **文章页** `blogs/article.html?id=N` 解析 `N` → `fetch('/blogs/articles/N.md')` → `marked` 渲染正文
3. **正文文件** `articles/N.md`：纯 Markdown，可混写原生 HTML（marked 原样透传）

### 文章记录格式（`文章.js`）

```
["标题", "介绍", "日期", [样式], [标签], "#编号", 阅读按钮, 跳转URL, 标题开关, 阅读时间, 时间开关]
```

| 字段 | 说明 |
| --- | --- |
| 阅读按钮 | `1` 显示"点击阅读"，`0` 隐藏 |
| 跳转URL | `"None"` 用默认编号页，其他字符串为外链/自定义跳转 |
| 标题开关 | `1` 自动设 `<title>标题 - Zhixie's Blogs</title>`，`0` 保留 HTML 硬编码 |
| 阅读时间 | 填分钟数；`时间开关` `1` = 按字数自动估算，`0` = 用填的分钟数 |

样式徽标定义在 `文章.js` 的 `STYLE_BADGES`（默认有 `固定` / `新` / `热`，可扩展）。

## 写新文章

**方式一：本地脚本（推荐）**

```bash
python blogs/create_article.py
```

交互式向导：`←` `→` 切换字段 → 填写 → 「生成文章」。自动写入 `blogs/articles/N.md` 并在 `文章.js` 注册。

**方式二：手动**

1. 新建 `blogs/articles/N.md`，写正文
2. 在 `文章.js` 的 `文章` 对象里加一条记录

## 本地预览

```bash
python blogs/test_server.py          # 默认端口 8000
python blogs/test_server.py 9000     # 指定端口
```

然后打开 <http://localhost:8000/blogs/index.html>

> 正文用 `fetch()` 拉 `.md`，**直接双击 html（file://）会被浏览器 CORS 拦截**，必须走 http 服务。

## 部署

push 到 `main` 分支即可，GitHub Pages 自动构建发布：

```bash
git add .
git commit -m "..."
git push
```

## 投稿

欢迎投稿！在仓库 **Issues** 里发一篇，把文章 Markdown 正文贴进去即可：

<https://github.com/uzjhf-836/uzjhf-836.github.io/issues/new>

人工审核后发布（你看到的正文会被原样保留，包括 Markdown 语法与原生 HTML）。
